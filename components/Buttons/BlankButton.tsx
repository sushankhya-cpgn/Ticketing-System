import * as React from 'react';
import Button from '@mui/material/Button';

export default function BasicButtons() {
  return (
    
      <Button variant="text" sx={{color:"bg--text-primary"}}>Text</Button>
  );
}